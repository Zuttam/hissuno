name: Tests

on:
  push:
    branches: [main]
    paths:
      - 'app/src/**'
      - 'app/package.json'
      - 'app/vitest.config.ts'
  pull_request:
    branches: [main]
    paths:
      - 'app/src/**'
      - 'app/package.json'
      - 'app/vitest.config.ts'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test -w app

      - name: Run tests with coverage
        run: npm run test:coverage -w app

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: app/coverage/
          retention-days: 14
